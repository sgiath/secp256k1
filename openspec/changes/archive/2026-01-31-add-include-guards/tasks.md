## 1. Add Include Guards

- [x] 1.1 Add include guard to `c_src/utils.h` using `SECP256K1_NIF_UTILS_H`
- [x] 1.2 Add include guard to `c_src/random.h` using `SECP256K1_NIF_RANDOM_H`

## 2. Verify

- [x] 2.1 Run `mix compile` to ensure NIFs still build correctly
- [x] 2.2 Run `mix test` to ensure no regressions
